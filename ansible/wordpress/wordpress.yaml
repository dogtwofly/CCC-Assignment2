---
- hosts: Wordpress
  vars_files:
    - host_vars/wordpress.yaml
  gather_facts: false
  pre_tasks:
    - name: 'Install Python3' 
      raw: sudo apt-get install update; sudo apt-get -y install python3
  roles:
    - role: common
    - role: docker
    - role: volumes
    - role: wordpress